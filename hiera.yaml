version: 4
datadir: hieradata
hierarchy:
  - name: "Nodes"
    backend: yaml
    path: "nodes/%{trusted.certname}"

  - name: "profiles"
    backend: yaml

  - name: "common"
    backend: yaml

  - name: "OS Family"
    backend: yaml
    path: "os/%{facts.os.family}"

  - name: "secure"
    backend: yaml
    path: "secure/%{trusted.certname}"

  - name: "secure/common"
    backend: yaml
