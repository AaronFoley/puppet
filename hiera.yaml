---
version: 4
datadir: hieradata
hierarchy:

  - name: "Secure"
    backend: yaml
    path: "secure"

  - name: "Node"
    backend: yaml
    path: "nodes/%{::trusted.certname}"

  - name: "Profile"
    backend: yaml
    path: "%{::calling_class_path}"

  - name: "Role"
    backend: yaml
    path: "roles/%{::role}"

  - name: "Common"
    backend: yaml
    path: "common"
